/**
 * Core Types — Re-export shim
 *
 * All domain types have been consolidated into @/types/index.ts.
 * This file re-exports everything for backward compatibility with
 * any imports that still use @/core/types.
 *
 * @module core/types
 * @deprecated Import directly from '@/types' instead.
 */

export * from '@/types';
